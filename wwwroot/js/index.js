function clearForm() {
    console.log("Start to clear the form of HTML.");
    document.getElementById("form_of_homepage").reset();
    document.getElementById("form_of_contacts").reset();
    console.log("Clear the form of HTML finish.");
}